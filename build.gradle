plugins {
    id "com.android.application" version "8.13.2" apply false
    // Flutter validates the host project's KGP even though the host sources are Java.
    id "org.jetbrains.kotlin.android" version "2.3.20" apply false
}
